import { SummeryTextPipe } from './summery-text.pipe';

describe('SummeryTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SummeryTextPipe();
    expect(pipe).toBeTruthy();
  });
});
